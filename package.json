{
  "scripts": {
    "build": "tsc",
    "fmt": "prettier --write .",
    "generate": "yarn build && node dist/generate.js",
    "getKey": "yarn build && node dist/getKey.js"
  },
  "dependencies": {
    "arweave": "^1.10.11",
    "arweave-mnemonic-keys": "^0.0.9",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.2.3"
  }
}
